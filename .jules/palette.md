## 2026-06-03 - CLI UI Defaults Exposure
**Learning:** Using `ArgumentDefaultsHelpFormatter` acts as a crucial UI enhancement for CLI tools, as showing defaults gives users immediate context without making them guess or consult external documentation.
**Action:** Always use `argparse.ArgumentDefaultsHelpFormatter` or an equivalent configuration in CLI tools to ensure default configurations are explicitly visible to the user.
